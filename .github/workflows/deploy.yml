name: Deploy to AWS Lambda
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up AWS CLI
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      - name: Deploy to AWS Lambda
        run: |
          zip -r function.zip .  # Package Lambda code into a zip file
          aws lambda update-function-code --function-name arn:aws:lambda:eu-north-1:771936571040:function:Contract_Analyzer --zip-file fileb://function.zip
#      - name: Rollback Lambda Function (if needed)
#        if: failure()
#        run: |
#          PREVIOUS_VERSION=$(aws lambda list-versions-by-function --function-name my-lambda-function --query "versions[-2]" --output text)
#          aws lambda update-function-code --function-name my-lambda-function --qualifier $PREVIOUS_VERSION --zip-file fileb://function.zip
